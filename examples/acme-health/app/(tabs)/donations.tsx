/**
 * Donations Screen - Preset Mode
 *
 * Uses the SDK's DonationsScreen preset, which wraps a WebView
 * pointing to the donations URL from config.
 */
export { DonationsScreen as default } from 'bot-sdk/screens';
